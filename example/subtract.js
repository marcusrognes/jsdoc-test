/**
 * Subtracts b from a
 *
 * @param {Number} a
 * @param {Number} b
 * @test
 * assert(subtract(2, 1)).toBe(1);
 * @test
 * assert(subtract(2,2)).toBe(0);
 */
function subtract(a, b) {
	return a - b;
}
