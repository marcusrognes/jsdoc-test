/**
 * Adds number a and b together
 * @param {Number} a
 * @param {Number} b
 * @test
 * assert(add(1, 2)).toBe(3);
 * assert(add(2, 3)).toBe(5);
 * @test
 * assert(add(1, 2)).toBe(5);
 */
function add(a, b) {
	return a + b;
}
